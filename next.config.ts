import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Hides the Next.js dev-mode indicator badge that otherwise renders in
  // the bottom-left corner of the app during `next dev`.
  devIndicators: false,
};

export default nextConfig;
